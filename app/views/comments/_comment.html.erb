<div class="content clearfix">
  <div class="comment_content">
    <dt><p class="comment_name">From  <strong><%= comment.name %></strong></p></dt>
    &nbsp;&nbsp;<dd><p class="comment_name"><%= comment.body %></p>
    <p class="comment_time"><%= time_ago_in_words(comment.created_at) %> Ago</p></dd>
    <p>
        <% if user_signed_in? && current_user.email=="priyanka.kadam615@gmail.com" %>
              <%= link_to 'Destroy Comment', [comment.post, comment],
                           method: :delete,
                           data: { confirm: 'Are you sure?' } %>
        <% end %>
 </p>
<hr></hr>
  </div>
</div>
